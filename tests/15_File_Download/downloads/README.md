# Downloads Folder

This folder is reserved for files produced or referenced by the file download lesson.

Generated download artifacts can change between runs and should stay isolated here or in the configured output folder instead of being mixed with source specs. The current local artifact filename is:

- `tta-notes.txt`
